# ============================================================================
# POS — Production stack
# ----------------------------------------------------------------------------
#   app        PHP-FPM + nginx + baked assets  (port 80)
#   queue      php artisan queue:work          (no port)
#   scheduler  loop schedule:run every 60s     (no port)
#   db         PostgreSQL 16                   (no port)
#   redis      Redis 7                         (no port)
#
# Source is baked into the image — no bind mounts, no host dependencies.
# All runtime values come from .env.production (single source of truth).
#
# Usage:
#   cp .env.production.example .env.production
#   # edit .env.production — set APP_KEY, DB_PASSWORD, MAIL_*, DOMAIN, etc.
#   docker compose --env-file .env.production -f docker-compose.prod.yml build
#   docker compose --env-file .env.production -f docker-compose.prod.yml up -d
#   docker compose --env-file .env.production -f docker-compose.prod.yml exec app php artisan migrate --force
# ============================================================================

x-app-build: &app_build
  build:
    context: .
    dockerfile: docker/prod/Dockerfile

services:
  app:
    <<: *app_build
    container_name: pos-app
    restart: always
    ports:
      - "${APP_PORT:-80}:80"
    env_file: .env.production
    volumes:
      - storage_public:/var/www/html/storage/app/public
      - storage_logs:/var/www/html/storage/logs
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1/up"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s

  queue:
    <<: *app_build
    container_name: pos-queue
    restart: always
    env_file: .env.production
    command: php artisan queue:work --sleep=3 --tries=3 --max-time=3600
    entrypoint: ["/bin/sh", "-c"]
    volumes:
      - storage_public:/var/www/html/storage/app/public
      - storage_logs:/var/www/html/storage/logs
    depends_on:
      app:
        condition: service_healthy
      redis:
        condition: service_healthy

  scheduler:
    <<: *app_build
    container_name: pos-scheduler
    restart: always
    env_file: .env.production
    command: >
      sh -c "while true; do
        php artisan schedule:run --verbose --no-interaction;
        sleep 60;
      done"
    entrypoint: ["/bin/sh", "-c"]
    volumes:
      - storage_public:/var/www/html/storage/app/public
      - storage_logs:/var/www/html/storage/logs
    depends_on:
      app:
        condition: service_healthy

  db:
    image: postgres:16-alpine
    container_name: pos-db
    restart: always
    environment:
      POSTGRES_DB: ${DB_DATABASE:-pos}
      POSTGRES_USER: ${DB_USERNAME:-pos}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - dbdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-pos} -d ${DB_DATABASE:-pos}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  redis:
    image: redis:7-alpine
    container_name: pos-redis
    restart: always
    command: redis-server --maxmemory 128mb --maxmemory-policy allkeys-lru
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

volumes:
  dbdata:
  redisdata:
  storage_public:
  storage_logs:
