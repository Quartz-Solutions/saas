# Approve all postinstall build scripts. Required for pnpm 10+, which otherwise
# errors out (ERR_PNPM_IGNORED_BUILDS) when transitive deps like unrs-resolver
# have postinstall hooks.
dangerously-allow-all-builds=true
